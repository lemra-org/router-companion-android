image: registry.gitlab.com/lemra/router-companion-android:latest

before_script:
  - chmod +x ./gradlew

build:
  script:
    - ./gradlew :app:assembleAmazonDebug :tasker-plugin:assembleAmazonDebug
  artifacts:
    paths:
    - app/build/outputs/
    - tasker-plugin/build/outputs

